services:
  - type: web
    name: gbts-bus-booking-app
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 3000
    healthCheckPath: /